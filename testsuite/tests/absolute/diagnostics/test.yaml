description: Test that the backend emits correct diagnostics in case an entry
  is ill formed, or there is an error while attempting to match a file.

resources:
  dummy_text_content: .

driver: python
